export class HelperFunctions {
  //   static numberWithCommas = (x: number) => {
  //     let x_new = x.toString();
  //     var pattern = /(-?\d+)(\d{3})/;
  //     while (pattern.test(x_new)) x_new = x_new.replace(pattern, "$1,$2");
  //     return x;
  //   };
}
